The performance of the target adapter is evaluated over three types of experiments.
In the first type, the PID controller's tuning is varied, while maintaining consistent KOS characteristics.
For the second group of experiments, the KOS characteristics are varied, namely the degree of perforation in the folds, while keeping the PID controller identical across tests.
Finally, the performance is tested for a crawling gait to observe the effect of the method in a highly restricted setting.

\subsection{Varying the PID Controller}\label{subsec:varying-pid}
For the first experiment, the method's robustness to the use of different PID controllers is tested.
The robot is equipped with the standard set of KOSs\todo{find a place to dicuss the specifics of the chosen KOS design}, using perforations of 1.5--0.5\si{mm} cut--skip ratio.
During this experiment, the values of the targets in the random sequence are integer values ranging between -3 and -25~degrees.
The robot has 5\si{\second} to reach and settle around a target before it is provided with the next target in the sequence.

\subsubsection{A controller that is slow but aggressive}
The first PID controller has a disproportionally high integral gain, resulting in \emph{aggressive} but \emph{slow to respond} behavior.
This controller yields particularly interesting results in combination with the bistable nature of the KOS.
\Cref{fig:slow-overshoot-ref} shows the signal response from the reference controller, illustrating the behavior of the controller.
In comparison, the signal obtained from the controller-adapter combination is shown in \Cref{fig:slow-overshoot-adapter}.

\begin{figure}
    \begin{subfigure}[t]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/slow-overshoot-ref}
        \vspace*{-2em}
        \caption{Reference signal.}
        \label{fig:slow-overshoot-ref}
    \end{subfigure}
    \begin{subfigure}[t]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/slow-overshoot-adapter}
        \vspace*{-2em}
        \caption{Target adapted signal.}
        \label{fig:slow-overshoot-adapter}
    \end{subfigure}
    \caption{Qualitative comparison of 1\si{\minute} of the signal response for the \emph{slow-aggressive} PID controller.}
    \label{fig:qualitaitve-aggressive}
\end{figure}

During the full 2\si{\hour} duration of the experiment, the evaluation of each 5\si{\minute} sequence was recorded.
The result can be seen in \Cref{fig:slow-overshoot-metrics}.

\begin{figure*}
    \centering
    \input{Thesis/figures/results/slow-overshoot-metrics.pgf}
    \vspace*{-1em}
%    \missingfigure[figwidth=\textwidth, figheight=0.5\linewidth]{metrics evolution an comparison for slow overshoot}
    \caption{Metrics evolution over the 2\si{\hour} duration of the experiment for an \emph{aggressive} but \emph{under-responsive} PID controller. Each point represents the calculated value of the metric on identical 5\si{\minute} sequences of targets. Numerical values can be found in~\Cref{app:results-ext}.}
    \label{fig:slow-overshoot-metrics}
\end{figure*}

Note the immediate (after the first 5\si{\minute}) improvement in overshoot and absolute error.
The controller with target adapter initially has high rise times and low overshoots, but then evolves toward a stable behavior that retains short rise time, small position errors, and consistent overshoot values.
As time progresses, the metric values for the unadaptive reference controller generally decrease, with the exception of the rise time.
The adapted controller, however, seems to achieve this level of performance from the start.

\paragraph*{Interpretation}
The conjecture is that the potential barrier between the two stable states of the KOS slows down the controller's response, resulting in a higher control input.
As the barrier is overcome, the KOS now complements the controller's action, resulting in the overshoot.
Depending on the vicinity of the target to the KOS's potential barrier, the controller can enter an unstable state.
As the potential barrier to transition between the two stable states of the KOS decreases, the reference controller has less difficulty maintaining a stable response.
This explains the steady decrease in settling time, absolute velocity, and total variance, which reflect the decreasing irregularities in the signal and the controller’s corrective actions.
The target adapter effectively shifts the target closer to the system's current state, facilitating a smoother transition to the desired state with reduced overshoot, thereby minimizing the risk of the controller entering an unstable cycle.

\subsubsection{An under-tuned controller}
The second PID controller is tuned with low gains, resulting in slow and under-responsive behavior.
\Cref{fig:under-tuned-ref,fig:under-tuned-adapter} display the comparison between the reference controller and the controller with target adapter, respectively.

\begin{figure}
    \begin{subfigure}[t]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/under-tuned-ref-signal}
        \vspace*{-2em}
        \caption{Reference signal.}
        \label{fig:under-tuned-ref}
    \end{subfigure}
    \begin{subfigure}[t]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/under-tuned-signal}
        \vspace*{-2em}
        \caption{Target adapted signal.}
        \label{fig:under-tuned-adapter}
    \end{subfigure}
    \caption{Qualitative comparison of 1\si{\minute} of the signal response for the \emph{under-tuned} PID controller.}
\end{figure}

The evolution of the metrics is shown in~\Cref{fig:under-tuned-metrics}.

\begin{figure*}
    \centering
    \input{Thesis/figures/results/under-tuned-metrics.pgf}
    \vspace*{-1em}
    \caption{Metrics evolution over the duration of the experiment for an \emph{under-tuned} PID controller. Each point represents the calculated value of the metric on identical 5\si{\minute} sequences of targets. Numerical values can be found in~\Cref{app:results-ext}.}
    \label{fig:under-tuned-metrics}
\end{figure*}

The controller with adapter clearly has lower rise times and absolute errors than the reference.
However, it also exhibits higher overshoots, whereas the reference controller even slightly undershoots toward the end of the 2\si{\hour} experiment.
The adapted controller results in higher mean absolute velocity and total variance.

\paragraph*{Interpretation}
The signal in~\Cref{fig:under-tuned-adapter} shows a clear reason for the lower rise times, namely that the target is shifted to more extreme positions as to yield larger control inputs from the controller.
This corresponds to the observed larger overshoot and higher mean absolute velocity.
The adapter makes the controller more \emph{aggressive}, as seen in the increased controller \emph{effort} (total variance).
However, the adapter also seems to introduce unnecessary adaptations once the steady state is reached, further increasing the mean absolute velocity.
These small oscillations are insignificant compared to the overall improvement in the mean absolute error.


\subsection{Varying the Perforation}\label{subsec:varying-kos}
For the second type of experiment, the PID controller is tuned to a highly responsive performance on a robot with KOS perforations of a 1.5--0.5~\si{\mm} skip-to-cut ratio.
The KOS perforations used are 0.25–0.25~mm, 0.5–0.5~mm, 1.5–0.5~mm, and 2--0.5~mm, representing various stages of KOS degradation.
Once again, the values of the targets in the random sequence are integer values ranging between -3 and -25~degrees, and the robot has 5\si{\second} to reach and settle around a target.

\subsubsection{0.25--0.25 \& 0.5--0.5 Perforations}
The 0.25--0.25\si{\mm} and 0.5--0.5\si{\mm} perforations represent KOSs in a state of lower degradation than the standard 1.5-0.5\si{\mm} perforation.
The evolution of the metrics over the course of the 1\si{\hour} experiment is shown in~\Cref{fig:25--25-50--50-metrics}, showing the average of the results from both experiments.

\begin{figure*}
    \centering
    \input{Thesis/figures/results/25--25-50--50-metrics.pgf}
    \vspace*{-1em}
    \caption{Metrics evolution over the 1\si{\hour} duration of the experiment for KOSs in a lower state of degradation (0.25--0.25\si{\mm} and 0.5--0.5\si{\mm} perforations, see annotations), with their average overlayed. Each point represents the calculated value of the metric on identical 5\si{\minute} sequences of targets. Numerical values can be found in~\Cref{app:results-ext}.}
    \label{fig:25--25-50--50-metrics}
\end{figure*}

On average, the adapted controller has lower rise times, settling times, and absolute errors, but higher overshoot, absolute velocity, and total variance.
Another observation is that the difference in the reference controller's performance between the two experiments drifts further apart over the full duration.
However, the adapted controller's performance over the two experiments appears less varied.
Overall, the relative difference in metrics between reference and adapted controller resembles that of the under-tuned PID experiment.

\paragraph*{Interpretation}
Since the perforations aim to represent a less degraded KOS, i.e.~stiffer, the gains of the PID (tuned to a 1.5--0.5\si{\mm} perforated KOS) are likely set too low.
The situation is thus analogous to the under-tuned PID experiment, and this shows in the behaviour of the target adapter:
The targets are shifted toward more extreme positions, resulting in the lower rise time.

%\begin{figure}
%    \begin{subfigure}[t]{\linewidth}
%        \centering
%        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/50--50-ref}
%        \vspace*{-2em}
%        \caption{Reference signal.}
%        \label{fig:50--50-ref}
%    \end{subfigure}
%    \begin{subfigure}[t]{\linewidth}
%        \centering
%        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/50--50-adapter}
%        \vspace*{-2em}
%        \caption{Target adapted signal.}
%        \label{fig:50--50-adapter}
%    \end{subfigure}
%    \caption{Qualitative comparison of 1\si{\minute} of the signal response for the 0.5--0.5\si{\mm} perforation.}
%    \label{fig:qualitaitve-50--50}
%    % TODO: Get in closer or remove figure
%\end{figure}


\subsubsection{1.5--0.5 \& 2--0.5 Perforations}
The 2--0.5\si{\mm} perforation aims to resemble a KOS that has been more deteriorated.
The PID controller is initially tuned for the 1.5--0.5\si{\mm} KOS.
For the 1.5--0.5\si{\mm} KOS experiment, with the runtime set to 2\si{\hour}, the aim is to assess how the controllers perform as the system gradually shifts into a more over-tuned condition.

\Cref{fig:150--50-200--50-metrics} shows that the general performance of the target adapter consists of higher settling times, overshoot, mean absolute velocity and total variance compared to the reference, especially as the experiments progress.
In fact, the only metric where the target adapter metrics are smaller than those of the reference, is the rise time.

\begin{figure*}
    \centering
    \input{Thesis/figures/results/150--50-200--50-metrics.pgf}
    \vspace*{-1em}
    \caption{Metrics evolution over the 2\si{\hour} duration of the experiment for KOSs in a higher state of degradation (1.5--0.5\si{\mm} and 2--0.5\si{\mm} perforations, see annotations), with their average overlayed. Each point represents the calculated value of the metric on identical 5\si{\minute} sequences of targets. Numerical values can be found in~\Cref{app:results-ext}.}
    \label{fig:150--50-200--50-metrics}
\end{figure*}

Inspecting~\Cref{fig:200--50-qualitative}, it can be observed that by the end of the experiment, the target adaptations have become significantly noisy, resulting in chaotic behavior.
The result of this is the increased settling time, mean absolute error, mean absolute velocity, and controller effort.
However, as shown in~\Cref{fig:150--50-200--50-metrics}, the adapted controller initially demonstrates better performance concerning settling time and mean absolute error.
Indeed,~\Cref{fig:200--50-adapter5} illustrates that, in the first 30\si{\minute} of the experiment, the target adapter produces minimal and relatively stable adaptations, leading to improved rise times and lower absolute error.


\begin{figure}
    \begin{subfigure}[tl]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/200--50-ref}
        \vspace*{-2em}
        \caption{Reference signal, late in the experiment (last 5\si{\minute} sequence).}
        \label{fig:200--50-ref}
    \end{subfigure}
    \begin{subfigure}[tl]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/200-50-adapter}
        \vspace*{-2em}
        \caption{Coresponding target adapted signal.}
        \label{fig:200--50-adapter}
    \end{subfigure}
    \begin{subfigure}[tl]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/200-50-adapter5}
        \vspace*{-2em}
        \caption{Target adapted signal, early in the experiment (sequence that ends at minute 30).}
        \label{fig:200--50-adapter5}
    \end{subfigure}
    \caption{Qualitative comparison of 30\si{\second} of the signal response for the 2--0.5\si{\mm} perforation.
    Also showing a comparison between the signals of the target adapted controller, late vs. early into the experiment.}
    \label{fig:200--50-qualitative}
\end{figure}

\paragraph*{Interpretation}
In the case of heavily deteriorated KOSs, the target adaptation seems to degrade the performance.
The initially improved rise times can be attributed to the highly oscillatory nature of the signal response, rather than genuine performance gains.
However, the fact that the target adapter shows improvement over the reference controller in the early stages of deterioration is promising.
The highly responsive PID controller is quick to react, reducing the need for extreme adaptations as seen in the previous experiments.
As the KOS further degrades, it sometimes enters an unstable cycle: much alike the ones seen in~\Cref{fig:slow-overshoot-ref},~\Cref{subsec:varying-pid}, but at a higher frequency.
This results in training data that contains many contradicting features, which results in increased noise in the adapter's predictions.
Noisy targets in turn produce noisy control actions and consequently noisy signal responses.

\subsection{Crawling Gait}\label{subsec:crawling-gait}
The final experiment emulates the mode of operation that the robotic crawler was designed for.
The targets in the sequences alternate between two angles on opposite sides of the potential barrier, specifically -8 and -18 degrees, mimicking the contraction and expansion typical of a crawling motion.
To introduce variability and avoid identical datapoints, a small normally distributed random offset is added to each alternating target.
The goal of this experiment is to evaluate how a constrained operational setting affects the adapter's compensation for the overshooting effect caused by the potential barrier.
The experiment uses a KOS with a perforation 1.5--0.5\si{\mm} and the highly responsive PID, tuned to this perforation.
Given the rapid degradation of the potential barrier in the early stages, the experiment only runs for 20\si{\minute}.
A reference controller is compared with two target adapter configurations: one pretrained and then fine-tuned online,
 and one that is entirely trained online.

\Cref{fig:crawling-metrics} shows the evolution of the metrics for the three runs over the span of 20\si{\minute}.
The results show consistently lower rise and settling times, and mean absolute error for both implementations of the target adapter compared to the reference controller.
These improvements come at the cost of significantly higher overshoot.

\begin{figure*}
    \centering
    \input{Thesis/figures/results/crawling-metrics.pgf}
    \vspace*{-1em}
    \caption{Metrics evolution over the 20\si{\minute} duration of the crawling-gait experiment, with the average of both adapted controllers overlayed. Each point represents the calculated value of the metric on identical 5\si{\minute} sequences of targets, in this case alternating between values near to -18 and -8 degrees, respectively. Numerical values can be found in~\Cref{app:results-ext}.}
    \label{fig:crawling-metrics}
\end{figure*}

Signal responses are shown in~\Cref{fig:crawl-qualitative}, note that the adapted signal is only shown for the fully online trained adapter, but it is very similar to that of the pretrained adapter.
The improvement in rise time is clearly observable, and shows that the improved settling time directly results from this, rather than the reference controller having an unstable signal.
In fact, the signal from the reference resembles that of an under-tuned controller.
Target adaptations appear to be minimal, being only visual in close-ups of the signal.
This corresponds to the relatively low difference in total variance between reference and adapted controllers, observed in the metric evolution. \todo{FOR ENTIRE SECTION: quantify the results: x\% improvement of y, etc.}  % TODO QUANTIFY!!!

\begin{figure}
    \begin{subfigure}[t]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/crawling-gait-ref}
        \vspace*{-2em}
        \caption{Reference signal.}
        \label{fig:crawl-ref}
    \end{subfigure}
    \begin{subfigure}[t]{\linewidth}
        \centering
        \includestandalone[build={latexoptions={"-output-directory=./Thesis/figures/results/"}}]{Thesis/figures/results/crawling-gait-adapter}
        \vspace*{-2em}
        \caption{Target adapted signal for the fully online trained adapter.}
        \label{fig:crawl-adapter}
    \end{subfigure}
    \caption{Qualitative comparison of 1\si{\minute} of the signal response for the 0.5--0.5\si{\mm} perforation.
    The snapshot is taken from the first 5\si{\minute} sequence.}
    \label{fig:crawl-qualitative}
\end{figure}

\paragraph*{Interpretation}
Although the adaptations made to the targets appear negligible for both the pretrained as the online trained adapter, the metrics and signal response show a definite improvement.
However, the cause of how these minor adaptations result in the observed improvements is not immediately apparent: the reference controller shows signs of being under-tuned, but the adapter does not produce adaptations similar to those of an under-tuned controller in~\Cref{subsec:varying-pid}.
On the other hand, the consistent improvements seen across both adapter configurations challenge the notion that these improvements are merely coincidental.
